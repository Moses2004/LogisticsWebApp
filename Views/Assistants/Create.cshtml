@model LogisticsWebApp.Models.Assistant

@{
    ViewData["Title"] = "Create New Assistant";
}

<style>
    /* General Body and Font - Keep consistent with other pages */
    body {
        font-family: 'Montserrat', sans-serif;
        background: linear-gradient(to right, #ece9e6, #ffffff);
        color: #34495e;
        margin: 0;
        line-height: 1.6;
    }

    /* Content Container - Consistent with other pages */
    .content-container {
        max-width: 700px; /* Adjust width for forms */
        margin: 40px auto;
        padding: 30px;
        background-color: #ffffff;
        border-radius: 12px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        border: 1px solid #e0e0e0;
    }

    /* Form Container - Consistent with Create New Order page */
    .form-container {
        padding: 20px;
    }

    /* Heading (H1) Styling - Consistent with Create New Order page */
    h1 {
        text-align: center;
        color: #2c3e50;
        margin-bottom: 35px;
        font-size: 2.2em;
        font-weight: 700;
        position: relative;
        padding-bottom: 10px;
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.05);
    }

        h1::after {
            content: '';
            position: absolute;
            left: 50%;
            bottom: 0;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background-color: #3498db;
            border-radius: 5px;
        }

    /* Form Group Styling */
    .form-group {
        margin-bottom: 25px;
    }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #34495e;
            font-size: 0.95em;
        }

    /* Input Field Styling */
    .form-control {
        width: 100%;
        padding: 12px 15px;
        border: 1px solid #ced4da;
        border-radius: 8px;
        font-size: 1em;
        color: #495057;
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
        box-sizing: border-box; /* Ensures padding doesn't add to total width */
    }

        .form-control:focus {
            border-color: #3498db;
            box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25); /* Blue glow on focus */
            outline: none;
        }

    /* Validation Message Styling */
    .text-danger {
        color: #dc3545; /* Red for errors */
        font-size: 0.85em;
        margin-top: 5px;
        display: block;
    }

    /* Submit Button Styling - Consistent with Create New Order button */
    .submit-button {
        display: block;
        width: 100%;
        padding: 15px 25px;
        background-color: #28a745; /* Green for Create/Save */
        color: white;
        text-decoration: none;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        font-size: 1.1em;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease;
        margin-top: 30px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

        .submit-button:hover {
            background-color: #218838; /* Darker green on hover */
            transform: translateY(-2px);
        }

    /* Back to List Link Styling */
    .back-link {
        display: block;
        text-align: center;
        margin-top: 25px;
        font-size: 1em;
    }

        .back-link a {
            color: #3498db;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

            .back-link a:hover {
                color: #2980b9;
                text-decoration: underline;
            }
</style>

<div class="content-container">
    <h1>Create New Assistant</h1>

    <div class="form-container">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" placeholder="e.g., Jane Smith" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone" class="control-label"></label>
                <input asp-for="Phone" class="form-control" placeholder="e.g., +0987654321" />
                <span asp-validation-for="Phone" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Age" class="control-label"></label>
                <input asp-for="Age" class="form-control" placeholder="e.g., 28" />
                <span asp-validation-for="Age" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="submit-button" />
            </div>
        </form>
    </div>

    <div class="back-link">
        <a asp-action="Index">Back to Assistant List</a>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}